using UnityEngine;

public class SceneRenamer : MonoBehaviour
{
    public void Awake()
    {
        //GameObject[] allObjects = UnityEngine.Object.FindObjectsOfType<GameObject>();
        //foreach (GameObject GO in allObjects)
        //{
        //    string _instanceID = "";
        //    _instanceID += GO.name + GO.transform.position.x + GO.transform.position.y + GO.transform.position.z + GO.transform.rotation.x + GO.transform.rotation.y + GO.transform.rotation.z + GO.transform.rotation.w; 
        //    GO.name = _instanceID.ToString();
        //}
    }
}
